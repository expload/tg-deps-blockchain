var init = require('./init');

describe('hooks', function() {
    before(function() {
      init();
    });
});

describe('TON Node Connection', function() {
    it('should be able to connect to local TON Node', function() {

    });
  });